import React, { Component } from 'react';
import ReactDOM from 'react-dom';

export default class Skeleton2 extends Component {
  constructor(props) {
    super(props);
  }

  render() {

    return (
      <div className="skeleton-component">
        This is the skeleton 2 component
      </div>
    );
  }
}
